# Jiffy SG — design brief

> ⚠️ **Dev/test only — NOT staged for student rollout.** Confirmed explicitly by the user 2026-07-24: despite the "working end-to-end" status below, Jiffy SG stays in `timeManagementDev/jiffySGDev/` (the "Develop" stage of the repo's three-stage pipeline) until further notice. Do not stage it into `shelfDev/` or `mpToolSet/`/`studio4AnimToolset/` without an explicit go-ahead — the current goal is just to keep it backed up on GitHub and accessible from RMIT machines for continued dev, not to ship it.

Status: **UI fork built and working end-to-end against real ShotGrid** (2026-07-24). `timeManagementDev/jiffySGDev/jiffySG.py` is now a full fork of `jiffyScheduleDev/jiffySchedule.py` (`JiffySG` class, `run_jiffySG()`), tested live inside Maya against the "Not Worth Fixing" test Project: adding/editing/deleting a Shot or Sequence automatically pushes to ShotGrid in the background (Shot/Sequence create, soft-delete, Frame Start/End → `Shot.cut_in`/`cut_out`, Stage/Due Date/Artist → a per-Shot Task under the "Animation" Pipeline Step). `SHOT_STAGES` was shrunk to match this site's real Task statuses (`Waiting to Start`/`In Progress`/`Final`) rather than requiring ShotGrid reconfiguration. See "UI fork, 2026-07-24" below for full detail, open items, and what's still out of scope (Assets-tab sync, Notes sync, `upload_playblast()`, the pbTool hand-off). RMIT's single shared ShotGrid site is confirmed; test site live at `https://nfw.shotgrid.autodesk.com`, official site rollout was slated ~2026-07-23 (not yet re-confirmed live). This doc captures the plan agreed 2026-07-15, auth findings from 2026-07-16, the connection test from 2026-07-24, and the UI fork milestone also from 2026-07-24, so the next session can pick it up without re-deriving it.

**Separately, 2026-07-24**: all 20 students on the `GRAP2596` class list (`aimStudio4_GRAP2596_3DcharacterPerformance` Project) were added as real ShotGrid accounts (Artist permission group) via ShotGrid's own CSV import — **not yet linked to that Project**, deferred to a future session. Note for next time: ShotGrid's import initially failed with `Aborted invite to Autodesk Identity: Invalid e-mail` when the CSV included Login/Permission Group columns; a minimal CSV with only First Name/Last Name/Email succeeded. Worth remembering if importing more students later — keep the import CSV to just name + email.

## What it is

A new tool, forked from JiffySchedule, that adds ShotGrid integration for RMIT's animation course. Not a feature bolted onto JiffySchedule — ShotGrid becomes mandatory and structurally central, which is different enough from JiffySchedule's local-only model to warrant its own tool. JiffySchedule keeps working untouched for as long as it's needed; Jiffy SG eventually replaces it once proven.

Goal: students manage everything — shot scheduling, status, notes — through Jiffy SG's UI. They never need to open ShotGrid directly.

## Context

RMIT gave the course ShotGrid access (2026-07-15). Every student gets their own account. The plan is to recommend RMIT stand up **one single shared site** (not one per staff member) with:
- **Staff** on the "Producer" permission group (stock ShotGrid role) — can create/manage Projects.
- **Students** on the "Artist" permission group, scoped to their own Project only.

Both are built-in ShotGrid permission groups — no custom permission engineering needed.

## Architecture

**Site URL** — one centrally-configured setting, not hardcoded in code (in case the single-site plan changes later) and not stored per-project (unnecessary complexity once it's confirmed as one shared site).

**Auth** — ~~per-student human login (`shotgun_api3`, `login`/`password`)~~ **superseded, see below.**

> ### Auth findings, tested 2026-07-16 against the test site
> **Direct username/password API login does not work.** Site's `user_authentication_method` is `oxygen` (Autodesk Identity/SSO), and it's enforced for real authenticated calls — `shotgun_api3.Shotgun(site_url, login=, password=)` followed by an authenticated call (e.g. `find`) throws `AuthenticationFault: Can't authenticate user`. (A first attempt looked like it succeeded because it only checked `sg.info()`, which doesn't require authentication at all — false positive, don't rely on `info()` as an auth test.)
>
> **Decided replacement: Script API key + `sudo_as_login`.** A single ShotGrid Script API key (site admin → API Script), bundled with Jiffy SG, authenticates all API calls. Per-student identity is applied via `shotgun_api3`'s `sudo_as_login` parameter. Verified from `shotgun_api3`'s own source docstring (`shotgun.py` ~line 561): `sudo_as_login` **applies that user's own permissions** to the action (so ShotGrid's Producer/Artist permission scoping still holds per-student, enforced by ShotGrid itself, not reimplemented in Jiffy SG) and **writes an event-log entry attributed to that student**, with an extra `sudo_actual_user` field noting the script performed it on their behalf. So the only thing actually lost versus true per-student login is *proof of identity at the point of use* — permissions and audit trail both stay correctly scoped to the real student.
>
> ### Real connection test — confirmed working, verified twice
> First proven 2026-07-21 alongside the `jiffySGDev` scaffold (commit `238fbf8`): `jiffySG.test_connection()` via `mayapy`, Script `JiffySG_002` + `sudo_as_login=patrick.sarell2@rmit.edu.au`, connected successfully, returned 17 visible Projects including the real course Project `VART3575` and a real student Project `Catherine_Vickery_S4096697` (the rest were ShotGrid's stock bundled templates).
>
> Re-verified 2026-07-24 on a different machine, after the original key turned out not to be saved anywhere — ShotGrid only shows the Application Key once, at creation; the admin panel's "masked" display afterward is not recoverable, regenerating is the only option. Same script name, freshly regenerated key, same login — connected successfully again via `mayapy` with `timeManagementDev/jiffySGDev` on `sys.path` (so the vendored `shotgun_api3` resolves) and a local config at `<Maya userPrefDir>/jiffySG_config.json`:
> ```json
> { "site_url": "https://nfw.shotgrid.autodesk.com", "script_name": "JiffySG_002", "api_key": "<never committed>" }
> ```
> This time 18 Projects were visible — one more than 07-21's count, almost certainly one of `Princess Peppercorn`, `Duck Duck`, `Bin Too Long`, or `Not Worth Fixing` (the user's own short-film test Project, created 2026-07-23; which exact one is still unconfirmed).
>
> **Identity-detection: OS-username auto-detect confirmed broken, on two separate machines.** `getpass.getuser()` returned `'scout'` on the 07-21 machine and `'patsa'` on the 07-24 machine — both rejected by ShotGrid (`AuthenticationFault: Cannot 'sudo' - unknown or retired user`). Only the full RMIT email (`patrick.sarell2@rmit.edu.au`) has worked, both times. This has only ever been tested against one staff/Producer account — a real student account is still needed to know whether *their* `login` field is also a full email, and whether it follows a predictable pattern against their RMIT domain username. **Fixed 2026-07-24 (later same day)**: `jiffySG.current_login()` now checks an optional `"sudo_as_login"` key in `jiffySG_config.json` first, only falling back to `getpass.getuser()` if that key or the whole config file is absent — so a per-machine override no longer needs `as_login=` on every call. Still just a manual override, not the automated OS-username→login resolution the plan below describes; that part remains unsolved.

> **Identity-detection plan (who to `sudo_as_login` as) — UPDATED 2026-07-24:**
> - **On RMIT lab machines**: ~~auto-detect the current OS/domain username (`getpass.getuser()` / `$env:USERNAME`) and use it directly as the ShotGrid login~~ — confirmed not viable as-is (see above, two-for-two failures). Either (a) map OS username → ShotGrid login via a lookup/pattern (e.g. `{username}@student.rmit.edu.au`, unconfirmed) once a real student `login` value is known, or (b) drop auto-detect entirely and use the same manual-entry/dropdown fallback already planned for home use. Needs a data point from an actual student account — both tests so far used the instructor's own staff account — before picking (a) vs (b).
> - **At home (no RMIT domain login)**: possible fallback — a student's Autodesk ID (used to activate their Education Maya license) might be the *same underlying identity* as their ShotGrid account, since both appear to sit on Autodesk's `oxygen` identity system. **Unconfirmed on two counts**: (1) whether it's literally the same account, not just the same auth backend, and (2) whether Maya/RV expose the currently-signed-in Autodesk ID programmatically to a script — needs research before relying on it. Until confirmed, home use likely needs a manual dropdown/override.
> - **Informal supporting signal (2026-07-16, needs formal confirmation)**: today, both the instructor and students logged into the ShotGrid website using the same username/password as their RMIT login — suggests RMIT's identity is unified across systems, but could also just be coincidental password reuse. Confirm properly with RMIT's IT/ShotGrid admin rather than relying on this alone.

> ### UI fork, 2026-07-24 — `jiffySG.py` is now a working Jiffy SG, not just a connection layer
> Ported `jiffyScheduleDev/jiffySchedule.py` (2583 lines) into `jiffySG.py` wholesale — `jiffySchedule.py` itself untouched, still works independently for students still on it. Assets tab, Progress/Milestones tab, Artists tab, and the existing "Jiffy Pomo" hand-off carried over **unchanged**. Only the Shots tab talks to ShotGrid. Class renamed `JiffySchedule`→`JiffySG`, launcher `run_jiffyschedule()`→`run_jiffySG()`, own data file (`jiffysg.json` via optionVar `jiffySGDataDir`, separate from JiffySchedule's `jiffyschedule.json`/`jiffyScheduleDataDir` — confirmed no cross-talk between the two tools running in the same session).
>
> **What pushes automatically** (instant, backgrounded on a daemon thread so the UI never blocks — mirrors `pbTool.py`'s `_push_to_rv` threading pattern):
> - Adding/editing/deleting a Shot → `create_shot()`/`delete_shot()` (soft-delete/retire, recoverable — was an explicit requirement: "as long as they can delete it if they've made a mistake").
> - Adding/removing a Sequence group (UI label changed from JiffySchedule's "Scene" to ShotGrid's own "Sequence") → `create_sequence()`/`delete_sequence()`, cascading to retire every Shot under a removed Sequence too.
> - Frame Start/End → `Shot.cut_in`/`cut_out` directly (no ShotGrid setup needed, worked first try).
> - Stage/Due Date/Artist → a single Task per Shot, created under a "Animation" Pipeline Step (chosen since none of the site's existing Shot-type Steps — Online/FX/Light/Comp/Layout/Tracking/Roto/Character FX — obviously fit a generic per-shot progress task). Due date converted DD-MM-YYYY→YYYY-MM-DD. Artist is resolved to a ShotGrid HumanUser by **exact name match** within the Project — Jiffy SG's Artist field is still local free-text (the brief's planned "read-only pull of ShotGrid project members" dropdown isn't built), so a typo'd/unmatched name just silently doesn't set an assignee, not an error.
> - A title-bar status dot (green/red/grey, next to the existing 📁 folder button) shows connection state and the last error's text as a tooltip — non-blocking, local editing keeps working even fully disconnected.
>
> **Stage list had to change**: ShotGrid's Task status codes on this site turned out fixed at `wtg`/`ip`/`fin` ("Waiting to Start"/"In Progress"/"Final") — not customisable to the original 6-value `SHOT_STAGES` (Previs/Blocking/Primary/Final/Rendered/Omit) the brief's Stage mapping section describes. Rather than have the user reconfigure ShotGrid, `SHOT_STAGES` was shrunk to `["Waiting to Start", "In Progress", "Final"]` (readable labels shown in the UI) with a `SHOT_STAGE_TO_SG_STATUS` dict in the backend converting to the real codes at push time. The Progress tab's "RENDERED" stat card and the Shots pie chart's completion calc were updated to use "Final" since "Rendered" no longer exists. **This is a real, permanent narrowing of Jiffy SG's stage granularity** — worth flagging if a future session wants the richer 6-stage list back, since that would need ShotGrid admin work (custom status codes) that was explicitly avoided here.
>
> **Verified working live**, against "Not Worth Fixing": Shot/Sequence create+delete (including cascade retire), frame range push, and Stage+Due Date+Artist push (confirmed via a direct query showing `sg_status_list: 'wtg', due_date: '2026-07-24', task_assignees: [Patrick Sarell]` on the created Task) — all via `mayapy` scripted checks. The live Maya UI itself was also exercised end-to-end by the user (add/edit/delete Shots and Sequences, status dot behavior) — same result.
>
> **Known limitation, accepted as-is**: rapid add-then-delete of a Shot/Sequence within the same short window the background create call takes to resolve can orphan a never-retired ShotGrid entity (the delete has nothing to retire yet). Not engineered around — consistent with the rest of the design's fire-and-forget philosophy, and soft-delete already makes stragglers admin-cleanable.
>
> **Still open / not built**: Assets-tab ShotGrid sync (out of scope, Shots only per this pass), Notes two-way sync (brief's original design, not started), `upload_playblast()` (still `NotImplementedError`, `pbtool-shotgrid-handoff-wip` branch still unmerged), Artist field validated against real ShotGrid project members instead of free text, and the OS-username identity-detection problem above (still just a manual config override).

> ### Real bug found and fixed, 2026-07-26 — background thread couldn't read the config
> User hit "no config file found at jiffySG_config.json" (bare filename, no path) inside the live Maya UI, and separately a persistent red status dot with the new-shot push silently failing — even though `mayapy.exe` standalone testing confirmed the config file was present, valid, and auth worked perfectly. Root cause: **every ShotGrid call (`test_connection`, `create_shot`, etc.) runs inside `_run_sg_async`'s background daemon thread** (jiffySG.py, "ShotGrid async helper"), including `_load_config()`'s call to `cmds.internalVar(userPrefDir=True)` buried inside `get_connection()`. `maya.cmds` is not safe to call off Maya's main thread — instead of raising, it silently returned `""`, so `_config_file()` built a bare relative `"jiffySG_config.json"` path (matching the exact bare-filename error text, which was the actual clue) instead of the real prefs path. This affected **every** ShotGrid push, not just the one being tested — almost certainly the actual cause of the earlier "stuck sending a new shot" symptom too, more likely than the project-name-mismatch theory floated first.
>
> **Fix**: added a module-level `_config_cache` in `jiffySG.py`, populated by `_load_config()` on first successful read. `_run_sg_async()` now calls `_load_config()` synchronously *before* starting the worker thread — since `_run_sg_async` is always invoked from Qt UI signal handlers on Maya's main thread, this is always safe, and the cache means the background thread itself never touches `cmds` again. Verified via `mayapy` (cache starts `None`, gets populated on the priming call, `test_connection()` still returns all 19 visible projects) and live in Maya — user confirmed green status dot + a new shot successfully pushed to ShotGrid after the fix.
>
> New dev convenience file: `timeManagementDev/jiffySGDev/launch_jiffySG.py` — paste-into-Script-Editor launcher that adds `jiffySGDev` to `sys.path` (needed since this folder isn't on Maya's default script path — it's deliberately not staged anywhere, per the dev/test-only note at the top of this brief) and force-reloads `jiffySG` so edits take effect without restarting Maya. Path is hardcoded per-machine; needs updating if run from a different clone location (e.g. an RMIT lab machine).

> ### Project linking + real Artist roster built, 2026-07-26 — driven by an imminent real need
> User is about to assign specific students to specific ShotGrid Projects for a major-projects class, which made two previously-deferred brief items ("Project linking", "Artist dropdown") urgent rather than hypothetical. Both built same day as the thread-safety fix above:
>
> - **`list_visible_projects(as_login=None)`** (new backend function) — wraps `sg.find("Project", ...)`, scoped by ShotGrid's own permissions for `as_login`. `test_connection()` now calls this internally instead of duplicating the query.
> - **NavPanel's "Add Project" is no longer free text.** `_add_project()` now emits `project_add_requested`; `JiffySG._on_project_add_requested()` (the parent, which owns the ShotGrid connection) queries `list_visible_projects()` async and presents only those names (minus ones already added) via a non-editable `QInputDialog.getItem` picker. `NavPanel.add_project(name)` is the new entry point that actually adds a validated name to the local list — the only way a new Project can be added at all now. This is the enforcement mechanism for "students should only be able to add shots to Projects they've been assigned to in ShotGrid": Jiffy SG does zero filtering itself, it only ever shows what ShotGrid's `sg.find` returns for that login.
> - **`list_project_artists(project_name, as_login=None)`** (new backend function) — `sg.find("HumanUser", [["projects","is",project]], ...)`, same query pattern `_resolve_artist` already used at push-time, now exposed as a first-class read.
> - **Artists tab rebuilt** — was a fully local, free-text add/edit/delete roster (name + role), completely disconnected from ShotGrid. Now read-only, live-pulled per active Project via `list_project_artists()`, with a "⟳ Refresh" button replacing "+ Add Artist". `_ArtistDialog` deleted outright (dead code); `_ArtistRow` simplified to a plain name-only display row, no edit/delete context menu — matches the brief's original "Jiffy SG never writes Project membership" design. No longer persisted locally (`artists` key dropped from `save_data()`/`load_data()`'s JSON payload) since it's always re-fetched live.
> - **Shot/Asset Artist dropdown (`ItemDialog`) is no longer editable free text** — `setEditable(True)` removed. The combo is now populated only from the active Project's real ShotGrid roster (via the same `set_artist_names()` plumbing, now fed by `ArtistsPage.artists_loaded` instead of the old local roster), plus an explicit blank "unassigned" entry and a preserved-but-not-retypable already-assigned name if it's fallen out of the roster. This closes the loophole that would otherwise have let a student type any name past the ShotGrid-membership restriction.
> - Verified via `mayapy`: `list_visible_projects()` → 19 projects (matches `test_connection()`'s existing count), `list_project_artists('Not Worth Fixing')` → `['Patrick Sarell']`. **Also confirmed live in the Maya UI, same session** — the "Add Project" picker and the Artists tab/Shot dialog dropdown both work as designed.
>
> **Known open gap, explicitly flagged by the user — verification pending, not a code defect**: every test of `list_visible_projects()` so far (this session and prior ones) has run `sudo_as_login`'d as the **instructor's own Producer-tier account** (`patrick.sarell2@rmit.edu.au`), which is why it always saw all 19 projects on the site. The actual restriction — that a student on ShotGrid's "Artist" permission group only sees Project(s) they're really a member of — depends entirely on how RMIT's ShotGrid site has permission/visibility rules configured; it is **not enforced by Jiffy SG's code** (by design — ShotGrid is meant to own this, not a reimplementation here) and has **never been tested against a real student account**. **User plans to test this directly with a real student login on Monday (~2026-07-27/28)** — until then, treat "students can only add Projects they're assigned to" as the intended/expected behavior per the site's permission design, not as independently confirmed.

> ### Shot thumbnail pushed to ShotGrid, 2026-07-26 (same session)
> User wants the thumbnail already captured/browsed in Jiffy SG's Shot row (`ThumbnailLabel` — viewport capture or "Browse for Image…") to also become that Shot's real ShotGrid thumbnail. New backend function `upload_shot_thumbnail(shot_id, image_path, as_login=None)` — thin wrapper around `sg.upload_thumbnail("Shot", shot_id, image_path)`, no-ops silently on a blank/missing path. Wired into both `_push_shot_create()` and `_push_shot_field_sync()` alongside the existing frame-range/Task pushes, so it fires on initial Shot creation and again on any later edit (same "cheap idempotent overwrite, don't bother diffing" philosophy already used for the other fields — a thumbnail gets silently re-uploaded on any unrelated row edit too, accepted as consistent rather than optimised). Verified via `mayapy`: blank/missing-path no-ops return `None` without erroring, and `sg.upload_thumbnail` is confirmed to exist on a live connection. **Also confirmed live in Maya, same session** — a captured/browsed thumbnail now shows up as the Shot's real thumbnail in ShotGrid.
>
> **User's assumption flagged, not yet true**: user expects the Shot's ShotGrid thumbnail to keep itself current automatically as playblasts get uploaded later, "I think that's just how ShotGrid works." It isn't automatic — ShotGrid doesn't cascade a `Version`'s thumbnail up to its linked `Shot` on its own. Documented directly in `upload_shot_thumbnail()`'s docstring as a note for whoever eventually builds `upload_playblast()` (still `NotImplementedError`): that function needs to explicitly call `upload_shot_thumbnail()` too, after uploading the Version's own thumbnail, or the Shot thumbnail will just stay frozen at whatever was captured in Jiffy SG.

> ### ⚠️ Security — needs active monitoring, not just at build time
> Bundling one Admin-tier Script API key into a tool distributed to a whole class is a materially different risk profile than per-student passwords: it's a single high-value credential, and if leaked it grants broad access to the entire RMIT site (not just one student's Project) unless carefully constrained. Needs an explicit plan before this ships, not just at final review:
> - Where the key lives at runtime (never committed to the repo; likely fetched from a local config file created by the installer, not hardcoded in `jiffySG.py`).
> - Whether the Script key can be scoped more tightly than full Admin (ShotGrid permission groups for Scripts, if available) while still supporting `sudo_as_login` for every student.
> - A rotation/revocation plan if the key is ever suspected leaked (e.g. found in a student's local files, posted somewhere).
> - Re-confirm real OS-username-based identity detection can't be trivially spoofed by a student on their own lab session (e.g. editing an environment variable) — worth a quick adversarial check once that code exists, not just assuming the OS username is trustworthy.

**Project setup** — 100% manual, done by the instructor directly in ShotGrid: creating each student's Project, adding students/mentors/helpers, both up front and as needed later. Jiffy SG never creates Projects or manages membership via the API.

**Project linking** — student logs into Jiffy SG, which queries which ShotGrid Project(s) that account can see and auto-links if there's exactly one (expected, since the instructor already scoped the Project to just that student). Re-linking uses the identical flow and must stay safely repeatable at any time — covers a site being rebuilt or a student's access changing, with no special-case handling needed.

**Shots/Assets** — created and scheduled entirely in Jiffy SG, pushed to ShotGrid:
| Jiffy SG field | ShotGrid field |
|---|---|
| Stage | `Task.sg_status_list` |
| Due Date | `Task.due_date` |
| Frame Start/End | Shot's built-in cut-in/cut-out fields |
| Artist | `Task.task_assignees` |

**Stage mapping** — ~~no translation table in code; the instructor sets ShotGrid Task status codes to match JiffySchedule's existing stage lists directly~~ **superseded 2026-07-24, see "UI fork" above**: the real site's Task statuses turned out fixed (`wtg`/`ip`/`fin`), not admin-configurable to a custom list, so this went the other direction — `SHOT_STAGES` was shrunk to match ShotGrid instead, with a `SHOT_STAGE_TO_SG_STATUS` translation dict in `jiffySG.py` doing the code conversion at push time. Assets stay unaffected (`WIP, Testing, Production Ready, Omit` — Assets aren't pushed to ShotGrid at all in this pass).

> ⚠️ Gotcha (still true, just resolved via the translation dict instead of manual configuration): ShotGrid's status field stores a short internal *code* separate from the display name shown in the UI. The **code** must match what Jiffy SG writes, not just the display label, or API writes will silently fail to land on the right status.

**Artist dropdown** — read-only pull of ShotGrid project members. Jiffy SG never writes project membership.

**Notes — genuine two-way sync** — uses ShotGrid's native `Note` entity as an append-only feed, not a single overwritable field. A note typed in Jiffy SG posts a new `Note`; a note added directly in ShotGrid (e.g. a mentor reviewing there) appears in Jiffy SG's feed on next pull. Nothing is ever overwritten in place, so there's no conflict-resolution problem to solve — merging two append-only lists just works.

**Packaging** — vendor `shotgun_api3` into `mpToolSet`, the same way ngSkinTools2 is already bundled (`mpToolSet/ngSkinTools2/`). No per-student pip install, no network dependency on lab machines.

## Rollout plan

1. ~~RMIT stands up the single shared site~~ — done; test site live, official site ~2026-07-23.
2. ~~Confirm SSO vs. direct-login~~ — done 2026-07-16: SSO-enforced, direct login doesn't work. Auth plan is now Script API key + `sudo_as_login` (see above).
3. **Auth confirmed working, verified twice (2026-07-21, 2026-07-24)** — see the "Real connection test" findings above for full detail. `timeManagementDev/jiffySGDev/jiffySG.py` (connection/auth layer only, not a `jiffySchedule.py` fork yet) and vendored `shotgun_api3` (`timeManagementDev/jiffySGDev/shotgun_api3/`, pulled from the official GitHub repo) are in place. Key lives only in `<Maya userPrefDir>/jiffySG_config.json`, never in this doc or committed.
   - **Real Project code found**: `VART3575` — almost certainly the actual course Project code the brief needed (the old saved-view URL `.../page/5779` wasn't it). `Catherine_Vickery_S4096697` also appeared, suggesting the per-student-Project naming pattern is already seeded on the site.
   - **Short-film test Project identified**: `Not Worth Fixing` — a real Maya project (`NWF_previs`) with a `scenes/sandbox/` folder now exists locally for testing against it.
   - Still open: confirm with RMIT's ShotGrid/IT admin — student `login` field ↔ RMIT domain username mapping (confirmed *not* OS-username-based for staff, on two machines; unconfirmed for students); whether an Admin-tier Script key is obtainable long-term or can be scoped down (this one was created/regenerated fine, but permission scope wasn't specifically checked); whether Autodesk ID is shared between Maya licensing and ShotGrid.
   - ~~Set up custom Task status codes on the `VART3575` Project matching `SHOT_STAGES`/`ASSET_STAGES`~~ — resolved differently 2026-07-24: rather than reconfiguring ShotGrid, `SHOT_STAGES` itself was shrunk to match the site's actual fixed statuses. See "UI fork, 2026-07-24" above. This was tested against `Not Worth Fixing`/`aimStudio4_GRAP2596_3DcharacterPerformance`'s shared site-wide status list, not `VART3575` specifically — worth a quick re-check there since Task statuses are usually site-wide, not per-Project, but not yet confirmed.
4. **`create_shot()` implemented and verified, 2026-07-24** — first real write path beyond auth. `create_shot(shot_code, project_name, sequence_name=None, as_login=None)` looks up the Project by name, optionally finds-or-creates a ShotGrid `Sequence` (via `_find_or_create_sequence()`), then finds-or-creates a `Shot` linked to it (`Shot.sg_sequence`) — idempotent, safe to call repeatedly. Deliberately minimal: no Task/status/frame-range fields yet, since Task status codes aren't set up on any real Project (see item 3).
   - **Naming decision**: uses ShotGrid's own entity/field names (`Sequence`, `sg_sequence`) directly, not JiffySchedule's "Scene" grouping label — a deliberate divergence from the rest of the toolset, decided because Jiffy SG's code talks to ShotGrid's schema constantly and a translation layer there just adds confusion.
   - **Verified two ways**: headless via `mayapy` (created `Shot` `ballisticsTest_001` under Sequence `sandbox` in `Not Worth Fixing`, id `1174`), then inside a real interactive Maya session via the Script Editor (created a second `Shot` `bigPigTest_001` under the same `sandbox` Sequence, confirming Sequence reuse works and that the whole loop — auth, Project lookup, Sequence reuse, Shot creation — behaves identically in-app, not just standalone).
5. **UI fork built and verified, 2026-07-24** — `jiffySG.py` is now a real, usable tool, not just a backend. See "UI fork, 2026-07-24" above for full detail. Next actual code piece: implement `upload_playblast()` for real (currently `NotImplementedError`) — the `pbtool-shotgrid-handoff-wip` branch (pbTool's side) is ready and waiting for it; needs a decision on Shot/Task lookup for a real course Project before it can create Version entities.
6. **Class roster imported, 2026-07-24 (separate from the tool work)** — all 20 `GRAP2596` students added as real ShotGrid accounts via CSV import, not yet linked to their Project. See the Status section at top for the import gotcha (keep CSVs to name+email only).
7. Pilot with the one existing 3-person team first — lower stakes, easier to debug auth/API rough edges before wider rollout.
8. Roll out to the rest of the class, each with their own ShotGrid Project (cloned from a template if useful, since ShotGrid's project-duplicate feature is admin-console-only and can't be triggered from a student's API session).
9. Once proven, retire JiffySchedule in favour of Jiffy SG.

**Bigger picture (per user, 2026-07-16):** this whole transition — Jiffy SG/ShotGrid, then the shared server, then eventually a render farm — is expected to roll out across the **whole school**, over roughly **3-4 years**. The concrete first slice within that is (a) get Jiffy SG talking to ShotGrid at all, then (b) roll that out to **all staff** before wider expansion — the single 3-student pilot above is the very first step of that, not something that needs to anticipate later stages (render farm, whole-school scale) yet. No rush intended on any of this; prioritize getting the foundations (auth, permissions, path handling) right over moving fast, since mistakes here compound across a much larger rollout later.

## Adjacent and future initiatives (added 2026-07-15)

Captured here so they inform Jiffy SG's design even though most are out of scope for the initial build.

**1. Playblast → Jiffy SG → ShotGrid.** pbTool (shipped RV playblast review launch 2026-07-15) should be able to send a playblast into ShotGrid as a `Version` entity linked to the shot's Task — ShotGrid's standard dailies/review workflow. Rather than pbTool holding its own separate ShotGrid login, it should hand the playblast off to Jiffy SG (same pattern as the existing JiffySchedule → JiffyPomo hand-off: a direct module call, e.g. `import jiffySG; jiffySG.upload_playblast(shot_name, filepath)`), and **Jiffy SG performs the actual upload using the session it already has open** for that student's linked project. This avoids a student needing to log into ShotGrid separately per tool, and keeps ShotGrid auth/session state in one place.

pbTool also needs two things at the point of sending a playblast:
- **A notes field**, so a student can add context to that specific playblast ("blocking pass, ignore the left arm"). Rather than inventing a second, separate notes system, this should feed the *same* append-only `Note`-entity feed already designed for Jiffy SG's shots — a playblast note is just another Note, linked to that Version (and/or the shot's Task), not a parallel concept.
- **Configurable burn-in info** — students should be able to control what text gets baked into the playblast video itself (name, shot, custom text), not just Maya's fixed built-in HUD elements. This is a Maya-technical question more than a ShotGrid one: Maya's native `playblast`/HUD ornaments only expose a fixed set of built-in fields (frame, camera, scene name), so genuinely custom freeform burn-in text needs either scripted custom HUD elements injected before the playblast runs, or a post-process compositing pass (e.g. ffmpeg `drawtext`) over the rendered video. Worth auto-populating the obvious fields (artist, shot name, stage) from Jiffy SG/ShotGrid data by default, with room for the student to type additional custom text on top — consistent with the rest of this design's theme of not making students retype what's already tracked.

> **pbTool's side done (2026-07-21), inert until Jiffy SG exists:** `pbTool.py` now has a "Notes (sent with this playblast)" scroll field, a "Custom Text" burn-in field (drawn bottom-right, since fps/scene/frame/focal already occupy the other three burn-in corners — not yet auto-populated from Jiffy SG/ShotGrid data since there's no session to pull from), and a "Send to ShotGrid" button wired to `send_to_shotgrid()`. That method does exactly the hand-off described above — `import jiffySG; jiffySG.upload_playblast(shot_name, version_folder, files=..., notes=...)` inside a try/except ImportError, same shape as `jiffySchedule.py`'s `_send_to_pomo` — and today always shows "Jiffy SG isn't installed" since `jiffySG.py` doesn't exist yet. `shot_name` is derived from `get_scene_relative_folder_from_scenes()`, and the payload passed is the raw JPEG version folder + file list (no video encoding step exists in pbTool) — whether Jiffy SG wants a pre-encoded movie or the frame sequence path is an open question for whoever builds `upload_playblast()`, not decided here.

**2. Portable drives → central server → render farm.** This ShotGrid rollout is step one of a bigger, multi-stage shift, and the eventual scale is **the whole school**, not just this course: (1) students move off portable/external drives onto (2) a single shared server, then eventually (3) a render farm. Not a Jiffy SG feature itself, but critical context — once there's a real shared server, uploaded playblasts/publishes can reference a consistent server path rather than a portable drive letter that won't resolve on anyone else's machine; further out, render farm job submission would likely tie into the same Task/Version tracking Jiffy SG already owns. Given the whole-school scale this eventually reaches, get the foundational pieces (auth, permissions, path handling) right now rather than retrofitting later — mistakes here compound across many more students down the line.

**3. mpAnim shelf: frame-range button.** A new shelf button that looks up the current shot's frame range (from Jiffy SG and/or ShotGrid) and sets Maya's playback range automatically, instead of an animator typing it in by hand. Needs to resolve "which shot is this" the same way JiffySchedule already infers a shot name from the current scene file. Should call into Jiffy SG's already-authenticated session (same reasoning as #1) rather than re-implementing its own ShotGrid connection.

**4. Long-term: multi-DCC bridge + full asset pipeline.** Vision beyond Maya — bridge ShotGrid to Adobe Premiere, After Effects, DaVinci Resolve, and Houdini, and extend tracking beyond shots to full asset pipeline management. This is a much larger, separate initiative that needs its own design pass when the time comes — not something to spec now. One real constraint worth flagging early: Houdini is Python-native like Maya, so the same connector approach likely extends there directly. Premiere and After Effects are not — they're scripted via JavaScript/ExtendScript/UXP, so bridging them to `shotgun_api3` (Python-only) needs a separate relay mechanism, not a direct port. DaVinci Resolve has its own Python/Lua scripting API, closer to feasible but still a distinct integration path. "Bridge to all four" is not the same amount of work in each case.

**Design implication for the initial build:** because pbTool and the frame-range shelf button both need ShotGrid access independently of Jiffy SG's own UI, **Jiffy SG should own the single ShotGrid session/connection for a given student**, with other tools calling into it rather than each maintaining separate logins and connector code. Worth designing Jiffy SG's connection layer with this in mind from the start, even though only the JiffyPomo-style hand-off exists today.
